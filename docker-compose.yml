version: "3.7"
services:
  node:
    build: ./back-end
    ports:
      - "3000:3000"
    volumes:
      - ./back-end/src:/app/src 
    env_file:
      - ./back-end/.env
    depends_on:
      - db
  db:
    image: mysql:8.0.23
    env_file:
      - ./back-end/.env
    ports:
      - "3306:3306"
